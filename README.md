# Installation steps
* Use npm

For running the project the first time, use the following command:

```shell
npm install
```

## Running the project

Use the following command:
```shell
npm start
```
